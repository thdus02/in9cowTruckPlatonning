# Python
__pycache__/   # 파이썬이 실행될 때 자동으로 생기는 캐시 폴더 (삭제해도 상관없음)
*.pyc          # 컴파일된 파이썬 바이트코드 (자동 생성됨)
*.pyo          # 최적화된 바이트코드 (거의 안 쓰이지만 혹시 몰라서 추가)

# Virtual env
venv/          # 프로젝트 내부에 가상환경을 만들어도 이건 올릴 필요 없음
env/           # 이름만 다르게 썼을 때 대비
pyvenv/        # 네 경우처럼 test1234 안에 있는 pyvenv
Lib/           # 가상환경의 라이브러리 폴더
Scripts/       # 가상환경 실행 파일 폴더 (activate.bat 등)
